# Skills de Matt Pocock

Copiadas de [mattpocock/skills](https://github.com/mattpocock/skills) (commit `c55ee46`, plugin v1.2.3), licencia MIT (ver `LICENSE-mattpocock-skills`).

Son las mismas 25 skills que instala el plugin `mattpocock-skills`, sin los archivos `agents/openai.yaml` (solo sirven para Codex).

- Grilling: `/grill-me` (sin estado) y `/grill-with-docs` (escribe `CONTEXT.md` y ADRs). Las dos usan `grilling` y `domain-modeling`.
- Flujo de desarrollo: `/to-spec` → `/to-tickets` → `/implement` (usa `tdd` y `code-review`), y `/wayfinder` para trabajos grandes.
- Corré `/setup-matt-pocock-skills` una vez para configurar el issue tracker y la ubicación de los docs.
- Si no sabés cuál usar: `/ask-matt`.

Para actualizar: volver a copiar desde el repo original.
